import 'package:flutter/material.dart';

class CapentryScreen extends StatelessWidget {
  const CapentryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}